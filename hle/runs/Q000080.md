# Run Q000080

- Date: 2026-09-01
- Category: Computer Science (decimal-architecture program design)
- Answer type: exact match
- Image: no
- Result: incorrect
- Stated confidence before reveal: low band, four-slot compound bound at an
  author-accounting prediction

## RIDM 18 Application

A fictional decimal architecture item: an exponential-decay scheduling
computation plus two meta-questions about the intended program, its variable
memory and its calls into a provided iterative logarithm. The frame question
(continuous versus stepwise decay) was settled by a tell inside the item
itself: the stepwise reading would need no calls to the expensive function
the item asks about, so coherence selected continuous decay. The two decay
times were derived with comfortable rounding margins, and the call count was
reconstructed exactly from the provided bisection code: per-call iteration
counts for the three logarithm arguments, summed under the time-efficient
caching program, landing on a round total that read as engineered.

Three of four slots matched the key, including the call-count decomposition.
The memory slot missed by two decimal digits: the candidate set enumerated
typed-input program variants but never generated the author's actual style,
reading every input as the fraction type and reusing dead input variables
for sums and the cached constant, storing no results at all. The committed
modal variant was two digits above the key; a reuse variant in the set was
four below it; the keyed variant itself was absent.

## Framework Observations

- Coverage myopia on the author-style axis: the ranking was careful over a
  candidate set that did not contain the true answer. The stem's own code
  samples were an available author-style corpus and were under-mined; the
  provided library code demonstrated the author's variable-reuse habits.
- The key's claimed minimum is itself beatable (a typed-input reuse variant
  totals less), so the slot graded is the author's construction, not an
  optimum, confirming that such slots are referent predictions and should
  be banded and enumerated as author-style questions, not derivations.
- The band discipline worked: the compound bound at the softest slot's
  audited class, the stated modal probability was honest, and the loss
  lands where the low band expects losses.
- The derivable slots were all secured by instrumenting or double-deriving
  (rounding margins checked, iteration counts computed from the given code
  rather than estimated).

## Lessons

1. On slots that grade an author's own construction (a program, an
   accounting, a recipe), the stem's example code and prose are the style
   corpus: enumerate the author's demonstrated idioms, uniform typing,
   variable reuse, expression-level results, as first-class candidate
   variants before ranking any modal reconstruction.
