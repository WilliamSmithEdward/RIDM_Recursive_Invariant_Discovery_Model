# Run Q000131

- Date: 2026-09-02
- Category: Computer Science/AI (boolean expression enumeration)
- Answer type: exact match
- Image: no
- Result: correct
- Stated confidence before reveal: high band, derivation-backed
  exhaustive enumeration with anchor validation

## RIDM 26 Application

A counting task over a small formal language: how many fixed-length
strings from a given symbol alphabet parse as boolean expressions
under stated operator precedence and evaluate true. The rigorous
route was mechanical: a recursive-descent parser for the canonical
grammar, calibrated on shorter lengths whose counts were first
derived by hand (anchor cases per the closed-world validation
ladder), then run over the full space. Every shape-class subtotal
in the enumeration was re-derived by hand before commitment.

The one referent fork worth pricing was whether the author counts
redundantly parenthesized forms as distinct expressions. The stem
settled it: the paren symbols sit in the given alphabet with
repetition explicitly licensed, so distinct strings count. The
excluding variant was still computed pre-reveal per the priced
referent-variant discipline, and not needed.

## Framework Observations

- The key's answer matched the enumeration exactly, and its listed
  witnesses confirm the same grammar convention (redundant parens
  counted, double negation allowed).
- Instrumented observation (Section 39) did the work: hand
  derivation established the anchors, the script established the
  count, and the two never disagreed at any length.

## Lessons

No new failure lesson. Win pattern: for closed formal-language
counts, build the parser, calibrate it on hand-derivable lengths,
and verify shape-class subtotals independently rather than trusting
one aggregate number.
